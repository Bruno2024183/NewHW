// Defining a variable 'age' of type number
let age: number = 30;

// Defining a variable 'name' of type string
let nm: string = "Alice";

// Logging the variables to the console
console.log(`Age: ${age}`);
console.log(`Name: ${nm}`);