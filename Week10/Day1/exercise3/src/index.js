"use strict";
// Union type variable 'id' that can hold either a string or a number
let id;
// Assign a string value to 'id'
id = "ABC123";
console.log(`ID as a string: ${id}`);
// Assign a number value to 'id'
id = 456;
console.log(`ID as a number: ${id}`);
